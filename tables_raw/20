%1
Let $S(n)$ be the statement

$$ \boldsymbol{1+2+\cdots+n = \frac{n(n+1)}2} $$

**for all positive integers $\boldsymbol n$**.
%2
Let $S(n)$ be the statement

$$ \boldsymbol{1+2+\cdots+n = \frac{n(n+1)}2}. $$

%3
$S(n)$ is a statement that depends on the value of $n$. We want to prove that $S(n)$ is true for all $n$.

%1
$\boldsymbol{ 1+2+\cdots+1 }$
%2
$\boldsymbol1$
%3
We start adding from 1 and end at $n$. When $n=1$, there is only one term and we should not write '$+2$' at all.

%1

$$\begin{align*}
	\boldsymbol{1+2+\cdots+1} & \boldsymbol{ {}=\frac{1(1+1)}2} \\
	\boldsymbol{1} & \boldsymbol{ {}=1}
\end{align*}$$

%2

$$\begin{align*}
	\textbf{LHS} & \boldsymbol{ {}=1} \\
	\textbf{RHS} & \boldsymbol{ {}=\frac{1(1+1)}2 = 1} \\
\end{align*}$$

%3
When proving an equality, we do not simplify both sides simultaneously. We may
either start from one side and reach the other, or simplify both sides
separately to obtain the same value or expression.

%1
Assume $S(k)$ is true for **all positive integers** $k$.
%2
Assume $S(k)$ is true for **some positive integer** $k$.
%3
'$S(k)$ is true for all $k$' is precisely the statement we need to prove, and
therefore it does not make sense to assume $S(k)$ is true for all $k$.

%1
**s.t.** $1+2+\cdots+k=\dfrac{k(k+1)}2$.
%2
**i.e.** $1+2+\cdots+k=\dfrac{k(k+1)}2$.
%3
Here we want to use the phrase 'that is' rather than 'such that', because we
want to explain what we mean by $S(k)$ is true, rather than to talk about some
consequences.

%1
When $\boldsymbol{S(k+1)}$
%2
When $\boldsymbol{n=k+1}$
%3
When we say 'when $n=\cdots$', we are discussing what happens under that
particular value of $n$. Here we need to prove that $S(k+1)$ is true, so we
cannot say 'when $S(k+1)$'.

%1
$\boldsymbol{ S(k+1)=1+2+\cdots+(k+1) }$
%2
**LHS** $\boldsymbol{ {}=1+2+\cdots+(k+1)}$
%3
$S(k+1)$ is a statement and cannot be equal to $1+2+\cdots+(k+1)$.

%1
Hence $\boldsymbol{n=k+1}$ is also true.
%2
Hence $\boldsymbol{S(k+1)}$ is also true.
%3
Only a statement can be true or false. $S(k+1)$ is a statement, but $n=k+1$ is not a statement since $n$ and $k$ are dummy variables and we cannot say $n=k+1$ is true or false.

%1
By the **principal** of mathematical induction, $P(n)$ is true for all $n\geq 2$.
%2
By the **principle** of mathematical induction, $P(n)$ is true for all $n\geq 2$.
%3
'Principal' is the head of a school and 'principle' is a general law or primary truth.
